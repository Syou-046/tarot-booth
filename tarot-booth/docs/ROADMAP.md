# ロードマップ

本番は 2026/10/17(土)。実機でのリハーサルを 10/10 までに一度やる前提で逆算している。日付は目安で、しょうの判断で動かしてよい。

担当欄が空のタスクは誰でも取れる。取るときは AGENTS.md の手順どおり、担当欄に名前を書いてそのコミットだけを先に入れる。「待ち」と書いたタスクは、理由が解消されるまで着手しない。

状態は `未着手` / `作業中` / `レビュー中` / `完了` / `待ち(理由)` のいずれか。

## P0 土台(完了)

| ID | 内容 | 担当 | 状態 |
|---|---|---|---|
| P0-1 | 試作を index.html / src / assets に分割、スモークテスト追加 | cowork | 完了 |

## P1 waltzingmouse 準拠で仕上げる(目安 9/30)

| ID | 内容 | 触るファイル | 担当 | 状態 |
|---|---|---|---|---|
| P1-1 | スプレッド変更の実装。選択肢と位置名は DECISIONS の記録どおり | src/app.js, src/share.js, index.html | | 待ち(スプレッド一覧の共有) |
| P1-2 | 78 枚の扇がタブレットで選びにくくないかを実機確認し、必要なら配置を調整(段数・カード幅・当たり判定)。抽選ロジックは触らない | src/app.js(fanLayout), src/styles.css | | 未着手 |
| P1-3 | 画像保存がタブレット(iPad Safari / Android Chrome)で動くか確認し、動かなければ直す | src/share.js | | 未着手 |
| P1-4 | 抽選の単体テスト。`rand()` の一様性(カイ二乗の簡易チェック)、`shuffle()` の重複なし、URL 復元の不正値拒否 | tests/ | | 未着手 |

## P2 ブースで確実に動かす(目安 10/7)

| ID | 内容 | 触るファイル | 担当 | 状態 |
|---|---|---|---|---|
| P2-1 | Service Worker で全ファイルをキャッシュし、一度開けば通信なしで動くようにする | sw.js(新規), index.html | | 未着手 |
| P2-2 | ホーム画面追加・全画面表示用の manifest とアイコン | manifest.webmanifest(新規), index.html | | 未着手 |
| P2-3 | ブースモードの確認。無操作リセット、誤操作でブラウザのメニューが出ないか | src/app.js | | 未着手 |
| P2-4 | 結果の持ち帰り方法の実装 | 未定 | | 待ち(持ち帰り方法の決定) |
| P2-5 | 公開先(GitHub Pages など)へのデプロイ手順を README に書く | README.md(新規) | | 未着手 |

## P3 実機リハーサル(10/10 まで)

| ID | 内容 | 担当 | 状態 |
|---|---|---|---|
| P3-1 | 本番のタブレットで、機内モードのまま 20 回連続で占い、止まる・崩れる箇所を記録する | しょう | 未着手 |
| P3-2 | P3-1 で出た問題の修正 | | 待ち(P3-1) |

## P4 公式サイト組み込み(上映会後)

| ID | 内容 | 担当 | 状態 |
|---|---|---|---|
| P4-1 | 公式サイト基盤に合わせた組み込み方式の決定と実装 | | 待ち(基盤の確認) |
| P4-2 | 高解像度カード画像への差し替え | | 待ち(入手元の決定) |
| P4-3 | OGP・共有まわり | | 待ち(P4-1) |
| P4-4 | 解読機能 | | 待ち(解読の扱いの決定) |

## 分担の目安

同じファイルを二人で同時に触らないことを最優先にする。目安として、画面と操作感(P1-1, P1-2, P2-3)は Claude Code、動作環境と検証(P1-3, P1-4, P2-1, P2-2, P2-5)は Codex に向いている。ただし固定ではなく、空いている方が取ってよい。
